{
  "name": "prsgroup",
  "version": "1.0.1",
  "description": "PRS GROUP Static Industrial Engineering Website",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -r *.html style.css main.js assets robots.txt sitemap.xml dist/",
    "start": "echo 'Static site: No build required' && exit 0"
  }
}
